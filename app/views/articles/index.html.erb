<table class="table">
    <thead class="thead-dark">
     <tr><th>Title</th><th>Category</th></tr>
    </thead>
    <% @articles.each do |article| %>
        <tr><td><%= article.title %></td><td><%= article.category.name %></td></tr>
    <% end %>
</table>